export const metadata = {
  title: "Polaris Studio",
  description: "Polaris 星图画布与 AI 节点工作台"
};

export default function StudioLayout({ children }: { children: React.ReactNode }) {
  return children;
}
