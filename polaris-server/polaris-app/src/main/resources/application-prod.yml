server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/polaris?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&autoReconnect=true&failOverReadOnly=false&maxReconnects=10
    username: polaris
    password: polaris123
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 300000
      connection-timeout: 20000
  data:
    redis:
      host: localhost
      port: 6379
      connect-timeout: 3000
      timeout: 3000
    rabbitmq:
      host: localhost
      port: 5672
      username: polaris
      password: polaris123
      connection-timeout: 5000
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 40MB

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl

minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin123
  bucket: polaris-assets

polaris:
  jwt:
    secret: k6n6grPQkMFGSAqp5llnakzhtYd/shXrT9RulRYm4+o=
    expiration: 86400000
  oss:
    type: minio

logging:
  level:
    org.springframework: WARN
    com.polaris: INFO
  file:
    name: ./logs/polaris-server.log
  logback:
    rollingpolicy:
      max-history: 7
      max-file-size: 50MB
      total-size-cap: 500MB
